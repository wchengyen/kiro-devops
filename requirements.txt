# 必需依赖
lark-oapi>=1.0.0
flask>=2.0.0
qrcode[pil]>=7.0
schedule>=1.0.0
cryptography>=3.0
pillow>=8.0
python-dotenv>=1.0.0
pyyaml>=6.0
pytz>=2023.3

# AWS 相关（实际必需：gateway 启动时会强制 import dashboard/resource_tree.py，其依赖 boto3）
boto3>=1.20.0

# 可选：AWS CLI（Kiro Skill 中执行 AWS 命令）
# awscli>=2.0.0
